import React from 'react';
import './Button.css';

const Button = ()=> {
    return(
        <button className="submitButton">
            Add to list
        </button>
    )
}

export default Button;